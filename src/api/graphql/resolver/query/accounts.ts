import {QueryResolvers} from "../../../../types/graphql";
import {ServerContext} from "../../../../types/apollo-server";

export const accountsResolver: QueryResolvers<ServerContext>['accounts'] = (parent, args, context, info) => {

}