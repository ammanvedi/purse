import {QueryResolvers} from "../../../../types/graphql";
import {ServerContext} from "../../../../types/apollo-server";

export const forecastResolver: QueryResolvers<ServerContext>['forecast'] = (parent, args, context, info) => {

}